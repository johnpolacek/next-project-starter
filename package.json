{
  "name": "project-starter",
  "description": "Get your next React project up and running quickly with Next.js, Theme UI, MDX and more.",
  "version": "0.1.0",
  "scripts": {
    "cypress": "cypress open",
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "format": "prettier --write \"**/*.{js,jsx,json,md}\"",
    "format-watch": "npm run format && onchange \"**/*.{js,jsx,json,md}\" -- prettier --write {{changed}}",
    "test": "cypress run",
    "deploy": "npm run test && git push origin master && now --prod"
  },
  "dependencies": {
    "@emotion/core": "^10.0.28",
    "@mdx-js/loader": "^1.5.8",
    "@next/mdx": "^9.3.2",
    "cypress": "^4.2.0",
    "next": "^9.3.2",
    "prop-types": "^15.7.2",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "theme-ui": "^0.3.1"
  },
  "devDependencies": {
    "onchange": "^6.1.0",
    "prettier": "^2.0.5"
  }
}
